# Proposal: Live Debate Dashboard & Self-Healing Agentic Loop (Combined Evolution)
Generated by: cm-brainstorm-idea
Date: 2026-05-18

## Why we're doing this
### Qualified Problem
**For:** AI Workflow Orchestrator (Multi-Agent System for Hackathon Track)
**Who:** Needs to demonstrate highly complex agentic adaptivity and adversarial alignment in a visually stunning, premium manner for the judges.
**The:** Live Debate & Self-Healing Control Room
**That:** Exposes the round-by-round debate steps, Agent Elo rating adjustments, and active self-healing/retry operations in real-time.
**Unlike:** Statically logs trace files to disk where they remain invisible, or simple console logs that lack premium user experience.
**Our approach:** We merge **Inicijativa A (Self-Healing Loop)** and **Inicijativa B (Live WebSocket/Event Stream Dashboard)** into a unified front-end visual interface backed by a high-frequency status event log.

---

### 9 Windows Analysis (TRIZ)

| Nivo Sistema | PAST | PRESENT | FUTURE |
| :--- | :--- | :--- | :--- |
| **SUPER-SYSTEM** | Single-agent execution pipelines without consensus. | Static MAS running on Cloud with background database tracking. | Fully observable agent societies with visual execution tracing. |
| **SYSTEM** | Console-based CLI executions. | Basic HTML web page with simple text-polling endpoint. | **Unified Control Room with Live Stream + Self-Healing controls.** |
| **SUB-SYSTEM** | Standalone log files (`.json`). | Static job status caching. | **Step-by-step Event log in JobManager + dynamic UI panels.** |

---

## What's Changing

We evaluated three different options for this evolution:
- **Option A: Visual Debate Board Only (WebSockets/SSE)** — Build a beautiful live card stream displaying agents talking in real-time. (Effort: M)
- **Option B: Autonomous Self-Healing Backend Only** — Build a recovery loop where agents retry failed terminal executions, saving debug traces to disk. (Effort: M)
- **Option C: Unified Control Room (Recommended)** — Implement backend-driven event streaming via status updates, and update the HTML frontend into a premium, responsive dashboard that visually models the round-by-round debate cards, dynamic ELO adjustments, and live self-healing operations when failures happen. (Effort: L)

### Why Option C is Recommended:
It provides the maximum possible value for a hackathon. It combines the extreme visual wow-factor of the live debate board with the deep architectural complexity of autonomous self-healing, presenting them as a single, high-fidelity experience.

---

## Proposed Changes

We will modify five key files to connect the frontend and backend event stream:

### 1. [MODIFY] [status_manager.py](file:///home/kizabgd/.gemini/extensions/ai-workflow-orchestrator/api/status_manager.py)
* Extend `JobManager` to support real-time events.
* Implement `add_event(job_id, status_type, payload)` to capture:
  * Category classification
  * Initial plan details
  * Argument generated (agent ID, role, round, content, confidence, dynamic Elo)
  * Execution status & validation reports
  * Self-healing/repair attempts and stdout/stderr traces.

### 2. [MODIFY] [rounds.py](file:///home/kizabgd/.gemini/extensions/ai-workflow-orchestrator/debate/rounds.py)
* Add a `job_id` parameter to `run_debate`.
* After each agent finishes its `think` call, push an event immediately to the `job_manager`:
  ```python
  if job_id:
      job_manager.add_event(job_id, "debate_round", {
          "agent_id": agent.agent_id,
          "role": agent.role.value,
          "round": arg.round,
          "content": arg.content,
          "confidence": arg.confidence,
          "elo": self.aggregator.get_agent_elo(agent.agent_id)
      })
  ```

### 3. [MODIFY] [engine.py](file:///home/kizabgd/.gemini/extensions/ai-workflow-orchestrator/orchestrator/engine.py)
* Propagate `job_id` through the 11-step pipeline.
* At each step (classification, plans, validation, audit), trigger `job_manager.add_event` to stream progress.
* Integrate self-healing directly: if validation checks fail or execute raises an exception, trigger the Critic/Solution loop to generate a fix and update the job trace.

### 4. [MODIFY] [server.py](file:///home/kizabgd/.gemini/extensions/ai-workflow-orchestrator/api/server.py)
* Update `/api/workflow` and `/api/status/{job_id}` to handle the new event list.
* Expose event arrays in status checks for real-time frontend rendering.

### 5. [MODIFY] [index.html](file:///home/kizabgd/.gemini/extensions/ai-workflow-orchestrator/static/index.html)
* Completely redesign the UI using a sleek, dark glassmorphic palette.
* Implement dynamic round-by-round cards that appear in real-time when the status is polled.
* Render a dynamic **Elo Leaderboard** showing agent standings after each run.
* Create a **Self-Healing Diagnostics console** that highlights fixes in green/red when they are generated.

---

## Verification Plan

### Automated Verification
- Run `pytest tests/test_agents.py` to ensure core systems remain solid.
- Build local test suites to verify that `add_event` successfully captures round-by-round states.

### Manual Verification
- Deploy to Hugging Face Spaces.
- Interact with the live Firebase frontend to trigger a workflow.
- Verify that cards pop up step-by-step, Elo ratings shift, and the dynamic panel works perfectly.
